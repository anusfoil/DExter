einops>=0.6,<0.8
h5py>=3.7
hydra-core>=1.3,<1.4
ipython>=8.0,<9.0
matplotlib>=3.5,<3.10
mir_eval>=0.7
numpy>=1.22,<1.27
omegaconf>=2.2,<2.4
pandas>=1.5,<3.0
partitura @ git+https://github.com/anusfoil/partitura.git@diffperformer_codec
pytorch_lightning>=1.9,<2.0
scikit-learn>=1.3,<1.6
scipy>=1.10,<1.15
seaborn>=0.12
# partitura still imports pkg_resources, which setuptools dropped in 81+
setuptools<81
torch>=1.13,<2.6
torchaudio>=0.13
# parangonar is needed by renderer.py; install separately with --no-deps because
# its newer versions force partitura>=1.6 and clash with the fork above:
#   pip install --no-deps parangonar
tqdm>=4.64
wandb>=0.15
